export enum ToastType {
  success = 'success',
  warning = 'warning',
  danger = 'danger'
}

export enum ToastPosition {
  top = 'top',
  middle = 'middle',
  bottom = 'bottom'
}


export enum ToastButtonSide {
  start = 'start',
  end = 'end'
}
